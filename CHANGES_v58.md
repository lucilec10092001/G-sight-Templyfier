# Templyfier v58

## Metrics without manual typing

- Kept the visible question-type recipes before the table.
- Made the table's **Select** checkboxes drive the metric editor immediately.
- One selected question edits that question only; several selected questions apply one recipe to the selected rows.
- Replaced free-text metric entry with a clickable multi-select populated from the real G-Sight metrics.
- Kept safe CATA response-code matching and leaves a target unchanged when correspondence is incomplete.
- Shows optional **Name shown in Excel** editing only for a single selected question.
- Removed the redundant question-specific Advanced metric editor from the user journey.
- Moved unrelated bulk actions into one collapsed optional section.

The calculation engine and Excel writer are unchanged.
